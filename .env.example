# Exemplo de variáveis de ambiente (copie para .env e ajuste).
# Caminho do artefato servível (default: models/pipeline.joblib)
CHURN_MODEL_PATH=models/pipeline.joblib
# Nível de log (DEBUG, INFO, WARNING, ERROR)
CHURN_LOG_LEVEL=INFO
